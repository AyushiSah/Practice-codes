#Making a matrix with 3 rows and 4 columns where each value is assigned to 0
matrix(0,3,4)
#Using a vector to initialise the matrix's values from 1 to 12
a<-1:12
print(a)

#Calling the matrix with the vector to replace the matrix's values from 0 to vector's values
matrix(a,3,4)
